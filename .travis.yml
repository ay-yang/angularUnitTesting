dist: trusty
sudo: false

language: node_js
node_js:
  - "8"

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
     - ./node_modules

before_install:
  - echo -e "machine github.com\n  login $GITHUB_TOKEN" > ~/.netrc
  - git lfs pull

install:
  - npm install

before_script:
  - yarn global add @angular/cli

jobs:
  include:
    - stage: "unit tests"
      script: npm run test -- --browsers=ChromeHeadlessCI
    - stage: deploy
      script: npm run start
    - stage: "e2e tests"
      script: protractor protractor-ci.conf.js
