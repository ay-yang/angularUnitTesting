dist: trusty
sudo: false

language: node_js
node_js:
  - "8"

addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable

cache:
  directories:
     - ./node_modules

before_install:
  - echo -e "machine github.com\n  login $GITHUB_TOKEN" > ~/.netrc
  - git lfs pull

install:
  - npm install

before_script:
  - yarn global add @angular/cli
  - npm run webdriver-update

jobs:
  include:
    - stage: "unit tests"
      script: npm run test -- --progress=false --watch=false --browsers=ChromeHeadlessCI
    - stage: "e2e tests"
      script: npm run start
    - script: npm run e2e -- --webdriverUpdate=false --devServerTarget=angular.io-example:serve --protractorConfig=protractor-ci.conf.js
    - stage: "e2e tests V2"
      script: npm run start
    - script: npm run protractor

notifications:
  email:
    - fmanrique@tangerinesoftware.com
    - yyang@tangerinesoftware.com

  on_success: always
  on_failure: always # default: always
